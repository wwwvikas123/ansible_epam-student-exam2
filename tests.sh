if [ $(curl -LI http://${EXTERNAL_ADDRESS}:${EXTERNAL_PORT} -o /dev/null -w '%{http_code}\n' -s) != "200" ]; then echo -e "service ${EXTERNAL_ADDRESS} does not response on port ${EXTERNAL_PORT}" && exit 1; fi